<Window x:Class="SpringLayout.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:diag="http://mindfusion.eu/diagramming/wpf"
        Title="MindFusion.Diagramming Sample: Spring Layout"
        Height="768" Width="1024" Icon="/App.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />            
            <RowDefinition Height="65"/>
        </Grid.RowDefinitions>

		<diag:DiagramView3D x:Name="view3D" />

		<Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Width="100" Margin="10" >
                <Button Content="Start"  Name="button1"  Click="button1_Click">

                </Button>
                <Button Content="Stop"  Name="button2"  Click="button1_Click">

                </Button>
            </StackPanel>
            
            <Border Grid.Column="1" Padding="5">
                <TextBlock TextWrapping="Wrap" Text="This sample demonstrates animated 3D SpringLayout. The nodes are displayed inside a DiagramView3D control. Drag with the mouse to pan the camera. Hold CTRL while dragging to rotate the camera.  Use the mouse wheel to move the camera back and forth."/>
            </Border>
        </Grid>
	</Grid>
</Window>
