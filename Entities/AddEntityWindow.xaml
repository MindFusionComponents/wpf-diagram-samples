<Window x:Class="MindFusion.Diagramming.Wpf.Samples.CS.Entities.AddEntityWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Add Entity" Height="320" Width="320" ResizeMode="NoResize">

	<Grid Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="Auto" />			
		</Grid.RowDefinitions>

		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="100" />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<TextBlock Margin="5" HorizontalAlignment="Right">Name:</TextBlock>
			<TextBox Name="txBName" Grid.Column="1" Margin="2">Entity</TextBox>

			<TextBlock Grid.Row="1" Margin="5" HorizontalAlignment="Right">Description:</TextBlock>
			<TextBox Name="txBDescription" Grid.Row="1" Grid.Column="1" Margin="2" />

			<Grid Grid.Row="2" Grid.ColumnSpan="2">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"  />
					<RowDefinition />
					<RowDefinition />
					<RowDefinition />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition />
					<ColumnDefinition />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>

				<TextBlock Grid.ColumnSpan="2" HorizontalAlignment="Left" Margin="5">Input</TextBlock>
				<TextBlock Grid.Column="2" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="5">Output</TextBlock>
				<CheckBox Name="row1Check" Grid.Row="1" Margin="5" Checked="OnCheck" Unchecked="OnCheck" IsChecked="True" />
				<CheckBox Name="row2Check" Grid.Row="2" Margin="5" Checked="OnCheck" Unchecked="OnCheck" IsChecked="True" />
				<CheckBox Name="row3Check" Grid.Row="3" Margin="5" Checked="OnCheck" Unchecked="OnCheck" IsChecked="True" />
				<TextBox Name="row1Edit" Grid.Row="1" Grid.Column="1" Margin="2" />
				<TextBox Name="row2Edit" Grid.Row="2" Grid.Column="1" Margin="2" />
				<TextBox Name="row3Edit" Grid.Row="3" Grid.Column="1" Margin="2" />
				<RadioButton Name="row1Radio" Grid.Row="1" Grid.Column="3" Margin="5" Checked="OnRadio" IsChecked="True" />
				<RadioButton Name="row2Radio" Grid.Row="2" Grid.Column="3" Margin="5" Checked="OnRadio" IsChecked="True" />
				<RadioButton Name="row3Radio" Grid.Row="3" Grid.Column="3" Margin="5" Checked="OnRadio" IsChecked="True" />
				<TextBox Name="row1OEdit" Grid.Row="1" Grid.Column="2" Margin="2" />
				<TextBox Name="row2OEdit" Grid.Row="2" Grid.Column="2" Margin="2" />
				<TextBox Name="row3OEdit" Grid.Row="3" Grid.Column="2" Margin="2" />

			</Grid>

		</Grid>

		<Button Name="btnAdd" Grid.Row="1" HorizontalAlignment="Center" Width="60" Margin="5" Click="OnAddBtn">Add</Button>

	</Grid>

</Window>
