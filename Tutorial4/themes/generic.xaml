<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:MindFusion.Diagramming.Wpf.Samples.CS.Tutorial4">

	<Style TargetType="local:OrgChartNode">
		<Setter Property="Template">
			<Setter.Value>
				<DataTemplate DataType="local:OrgChartNode">
					<Grid>

						<Rectangle
							Stroke="{Binding Stroke}"
							Fill="{Binding Brush}" />

						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="80" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>

							<Image
								Source="{Binding Path=Image}"
								HorizontalAlignment="Stretch"
								VerticalAlignment="Stretch"
								Stretch="Uniform">
							</Image>

							<StackPanel Margin="4,8,0,0"  Orientation="Vertical" Grid.Column="1">
								<TextBlock Text="{Binding Title}" FontWeight="800" Foreground="Black" />
								<TextBlock Text="{Binding FullName}" Foreground="Blue" />
								<TextBlock Text="{Binding Text}" FontSize="9" Foreground="Black" />
							</StackPanel>
						</Grid>

					</Grid>
				</DataTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>