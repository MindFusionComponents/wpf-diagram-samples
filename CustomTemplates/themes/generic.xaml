<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="clr-namespace:MindFusion.Diagramming.Wpf.Samples.CS.CustomTemplates">

	<Style TargetType="local:OrgChartNode">
		<Setter Property="Template">
			<Setter.Value>
				<DataTemplate DataType="local:OrgChartNode">
					<Grid>

						<Rectangle
							Stroke="{Binding Stroke}" StrokeThickness="{Binding StrokeThickness}"
							Fill="{Binding Brush}" RadiusX="10" RadiusY="10" />

						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="80" />
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition />
							</Grid.ColumnDefinitions>

							<Grid Margin="8">
								<Grid.RowDefinitions>
									<RowDefinition />
									<RowDefinition Height="Auto" />
									<RowDefinition Height="18" />
								</Grid.RowDefinitions>

								<Image Source="{Binding Path=Image}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="Uniform" />

								<TextBlock HorizontalAlignment="Center" Text="Role:" FontFamily="Verdana" FontSize="12" Foreground="DimGray" Grid.Row="1" />

								<StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
									<Button Name="PreviousImageButton" Padding="2" Margin="0,0,1,0" Width="18">
										<Button.Content>
											<Path Width="6" Height="12" Data="M0,50 L100,100 L100,0 z" Stretch="Fill" Fill="Black" Stroke="Transparent" IsHitTestVisible="False" />
										</Button.Content>
									</Button>
									<Button Name="NextImageButton" Padding="2" Width="18">
										<Button.Content>
											<Path Width="6" Height="12" Data="M100,50 L0,100 L0,0 z" Stretch="Fill" Fill="Black" Stroke="Transparent" IsHitTestVisible="False" />
										</Button.Content>
									</Button>
								</StackPanel>
							</Grid>

							<Border Padding="0,10,0,10" Grid.Column="1">
								<Border Background="Gray" Width="4" />
							</Border>

							<Grid Grid.Column="2" Margin="8">
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto" />
									<RowDefinition Height="Auto" />
									<RowDefinition />
									<RowDefinition Height="Auto" />
								</Grid.RowDefinitions>
								<Border Padding="2,0,2,4" Grid.Row="0">
									<TextBlock FontFamily="Verdana" FontSize="16" FontWeight="Bold" TextAlignment="Left" Text="{Binding Title}" />
								</Border>
								<Border Padding="2,0,2,4" Grid.Row="1">
									<TextBox AcceptsReturn="False" FontFamily="Verdana" FontSize="12" BorderBrush="Transparent" Background="Transparent" Text="{Binding FullName}" />
								</Border>
								<Border Padding="2,0,2,4" Grid.Row="2">
									<TextBox FontFamily="Verdana" FontSize="12" AcceptsReturn="True" BorderBrush="Transparent" Background="Transparent" TextWrapping="Wrap" Text="{Binding Text}" />
								</Border>
								<Border Padding="2,2,2,0" Grid.Row="3">
									<Button Name="AddSubordinateButton" Content="Add Subordinate" Padding="2" HorizontalAlignment="Right" />
								</Border>
							</Grid>
						</Grid>

					</Grid>
				</DataTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
